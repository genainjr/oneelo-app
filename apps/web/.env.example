NEXT_PUBLIC_API_URL=http://localhost:4001

# Rollout do primeiro acesso. Ausente ou false libera o destino normal para todos.
# Requer novo build do frontend quando alterado.
NEXT_PUBLIC_ONBOARDING_ENABLED=false

# Rollout do login por telefone e senha. Requer novo build quando alterado.
NEXT_PUBLIC_PHONE_PASSWORD_LOGIN_ENABLED=false

# PWA / Push notifications
# O frontend nao deve receber a WEB_PUSH_PRIVATE_KEY.
# A chave publica VAPID e lida da API autenticada em /api/notifications/public-key.

# Opcional para testar o Next dev server a partir de outro dispositivo na rede local.
# Exemplo: NEXT_ALLOWED_DEV_ORIGINS=192.168.0.14
NEXT_ALLOWED_DEV_ORIGINS=
